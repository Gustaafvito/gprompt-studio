# ============================================================
# G-Prompt Studio v1.0 — Dependencias
# ============================================================
# Instalación básica (solo obligatorias):
#   pip install -r requirements.txt
#
# Con todas las opcionales:
#   pip install -e ".[all]"
# ============================================================

# ── Obligatorias ──────────────────────────────────────────
customtkinter>=5.2.0
Pillow>=10.0.0
requests>=2.31.0
python-dotenv>=1.0.0
pyperclip>=1.8.0
google-genai>=0.1.0
openai>=1.0.0

# ── Recomendadas (mejoran UX, no rompen si faltan) ─────────
CTkToolTip>=0.8

# ── Opcionales: descomenta lo que necesites ────────────────

# Soporte Claude (Anthropic)
# anthropic>=0.18

# Almacenamiento seguro de API keys (keyring del SO)
# keyring>=24.0

# Notificaciones del sistema operativo
# plyer>=2.1

# Grabación de vídeo de sesión (modo tutorial)
# mss>=9.0
# imageio[ffmpeg]>=2.30
# numpy>=1.24

# ── Solo para desarrollo ─────────────────────────────────
# pytest>=7.0
# pytest-cov>=4.1
# mypy>=1.5
